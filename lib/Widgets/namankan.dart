import 'package:flutter/material.dart';


class Namankan extends StatelessWidget {
  const Namankan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      child: Text("hello"),
    );
  }
}
